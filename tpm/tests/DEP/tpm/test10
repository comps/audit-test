###############################################################################
# (c) Copyright Hewlett-Packard Development Company, L.P., 2008
###############################################################################

Test10 ()
{
	#
	# ID this function with -id argument
	#
	[ "$IDFLAG" ] && {
		echo "Test10 - Tspi_TPM_Delegate_CreateKeyDelegation01"
		return 0
	}

	echo
	echo -n "T10: Create Key Delegation..."
	$TESTPATH/tpm/Tspi_TPM_Delegate_CreateKeyDelegation01 $VERSION 2>>eout >>sout
	case "$?" in
	0 )
		echo "successful."
		return 0
		;;
	* )
		echo "Failed($ESTAT)."
		echo "-> Check `pwd`/eout for details"
		;;
	esac

	return 1
}
