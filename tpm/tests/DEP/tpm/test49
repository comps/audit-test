Test49 ()
{
	#
	# ID this function with -id argument
	#
	[ "$IDFLAG" ] && {
		echo "Test49 - Tspi_TPM_SetStatus01"
		return 0
	}

	echo
	echo -n "T49: Tspi_TPM_SetStatus01..."
	$TESTPATH/tpm/Tspi_TPM_SetStatus01 $VERSION 2>>eout >>sout
	case "$?" in
	0 )
		echo "successful."
		return 0
		;;
	* )
		echo "Failed($ESTAT)."
		echo "-> Check `pwd`/eout for details"
		;;
	esac

	return 1
}
