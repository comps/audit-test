Test33 ()
{
	#
	# ID this function with -id argument
	#
	[ "$IDFLAG" ] && {
		echo "Test33 - Tspi_EncodeDER_TssBlob01"
		return 0
	}

	echo
	echo -n "T33: Tspi_EncodeDER_TssBlob01..."
	$TESTPATH/tspi/Tspi_EncodeDER_TssBlob01 $VERSION 2>>eout >>sout
	case "$?" in
	0 )
		echo "successful."
		;;
	* )
		echo "Failed($ESTAT)."
		echo "-> Check `pwd`/eout for details"
		;;
	esac

	return 0
}
