Test14 ()
{
	#
	# ID this function with -id argument
	#
	[ "$IDFLAG" ] && {
		echo "Test14 - Tspi_TPM_GetAuditDigest01"
		return 0
	}

	echo
	echo -n "T14: Locate current audit digest..."
	$TESTPATH/tpm/Tspi_TPM_GetAuditDigest01 $VERSION 2>>eout >>sout
	case "$?" in
	0 )
		echo "successful."
		return 0
		;;
	* )
		echo "Failed($ESTAT)."
		echo "-> Check `pwd`/eout for details"
		;;
	esac

	return 1
}
