Test16 ()
{
	#
	# ID this function with -id argument
	#
	[ "$IDFLAG" ] && {
		echo "Test16 - Tspi_TPM_GetEvents01"
		return 0
	}

	echo
	echo -n "T16: Get an array of events..."
	$TESTPATH/tpm/Tspi_TPM_GetEvents01 $VERSION 2>>eout >>sout
	case "$?" in
	0 )
		echo "successful."
		return 0
		;;
	* )
		echo "Failed($ESTAT)."
		echo "-> Check `pwd`/eout for details"
		;;
	esac

	return 1
}
